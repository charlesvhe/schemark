---
reporter: 测试-赵琳
assignee: 王浩
critical: 阻塞
status: 进行中
tags:
  - 订单
  - 幂等
ref-task:
  - T0007
---

# 高并发下重复创建订单

## 现象

压测脚本以同一 `Idempotency-Key` 并发 100 次请求时，仍创建了 3 条订单。

## 怀疑根因

幂等键的写入没有放在事务起始，存在 race。

## 计划

将 `Idempotency-Key → order_id` 的映射改为唯一索引落库后才进入下游逻辑。
